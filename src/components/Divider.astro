<hr class="wavy-line" />

<style>
    hr {
        margin: 2.5rem 0;
    }
    .wavy-line {
        --s: 2.5px; /* size of the wave */
        --b: 1px; /* thickness of the line */
        --m: 0.7; /* curvature of the wave [0 2] */

        background: var(--secondary-text);
        --R: calc(var(--s)*sqrt(var(--m)*var(--m) + 1) + var(--b)/2);
        height: calc(2*var(--R));
        width: 100%;
        --_g: #0000 calc(99% - var(--b)), #000 calc(101% - var(--b)) 99%, #0000 101%;
        mask:
            radial-gradient(var(--R) at left 50% bottom calc(-1*var(--m)*var(--s)), var(--_g)) 
            calc(50% - 2*var(--s)) calc(50% - var(--s)/2 - var(--b)/2)/calc(4*var(--s)) calc(var(--s) + var(--b)) repeat-x,
            radial-gradient(var(--R) at left 50% top calc(-1*var(--m)*var(--s)),var(--_g)) 
            50% calc(50% + var(--s)/2 + var(--b)/2)/calc(4*var(--s)) calc(var(--s) + var(--b)) repeat-x;
    }

    html[data-theme="dark"] .wavy-line {
        background: var(--dark-secondary-text); /* Use a dark theme color for the wavy line */
    }

    @media (forced-colors: active) {
        .wavy-line {
            mask: none; /* Remove the SVG mask to ensure a solid line */
            background-color: CanvasText; /* Use a system color for the line's body */
            height: 1px; /* Ensure a standard, clear thickness for the separator */
            border: none; /* Remove any default or conflicting borders */
        }
    }
</style>
